// math.flux
// This is a Flux Library. It doesn't do anything on its own.

mut PI = 314159

mut square = fn(x) {
    return x * x
}

mut add = fn(a, b) {
    return a + b
}

mut describe = fn() {
    print("Standard Math Library v1.0")
}